apiVersion: 1

providers:
  - name: 'Dashboards por Defecto'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Esta es la ruta DENTRO del contenedor de Docker
      path: /etc/grafana/provisioning/dashboards